// const dataExports: { [k: string]: object } = {}
// for (const [locale, data] of Object.entries(exports)) {
//   const localeData: { [k: string]: any } = (dataExports[locale] = {})
//   for (const [key, val] of Object.entries(data as { [k: string]: any })) {
//     localeData[`${hash}_${key}`] = val
//   }
// }
